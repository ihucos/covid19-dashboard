language: generic
dist: bionic

install:
  - sudo apt-get install python3-pandas
  - sudo snap install jsonnet

script:
  - make clean
  - make

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
